# Termux 编译方案汇总

## 方案一：在 Termux 中编译（最简单）✅

**优点**：最简单，不需要交叉编译
**缺点**：需要在 Android 设备上操作

```bash
cd ~/Termux脚本
bash install-termux.sh
```

---

## 方案二：使用 WSL（Windows Subsystem for Linux）🔧

如果你有 WSL，可以在 WSL 中编译：

```bash
# 在 WSL 中
cd /mnt/d/桌面/fatalder/Termux脚本
export GOPROXY="https://goproxy.cn,direct"
go mod download
GOOS=android GOARCH=arm64 go build -ldflags="-s -w" -o fatalder-termux main.go
```

---

## 方案三：使用代理/VPN 🌐

在 Windows PowerShell 中设置代理后编译：

```powershell
# 设置系统代理（如果你有代理）
$env:HTTP_PROXY="http://127.0.0.1:7890"  # 替换为你的代理地址
$env:HTTPS_PROXY="http://127.0.0.1:7890"

# 或者设置 Go 代理
$env:GOPROXY="https://goproxy.cn,direct"

# 然后编译
cd "d:\桌面\fatalder\Termux脚本"
$env:GOOS = "android"
$env:GOARCH = "arm64"
$env:CGO_ENABLED = "0"
go mod download
go build -ldflags="-s -w" -o fatalder-termux main.go
```

---

## 方案四：手动下载依赖包 📦

如果网络问题持续，可以手动下载依赖：

```powershell
# 1. 先尝试下载（可能会失败，但会显示需要哪些包）
cd "d:\桌面\fatalder\Termux脚本"
go mod download

# 2. 如果失败，查看 go.mod 文件，手动下载需要的包
# 3. 放到 $env:GOPATH/pkg/mod 目录下
```

---

## 方案五：使用 GitHub Actions 在线编译 ☁️

创建一个 GitHub Actions 工作流，让 GitHub 帮你编译：

1. 将代码推送到 GitHub
2. GitHub Actions 自动编译
3. 下载编译好的文件

---

## 方案六：使用 Docker 🐳

如果有 Docker，可以在容器中编译：

```bash
docker run --rm -v "$(pwd):/workspace" -w /workspace golang:latest \
  bash -c "export GOPROXY=https://goproxy.cn,direct && \
  GOOS=android GOARCH=arm64 go build -ldflags='-s -w' -o fatalder-termux main.go"
```

---

## 方案七：直接使用 Go 源码运行（不编译）💡

在 Termux 中直接运行 Go 源码（需要安装 Go）：

```bash
# 在 Termux 中
cd ~/Termux脚本
go run main.go
```

但这样每次运行都需要编译，比较慢。

---

## 方案八：使用 Termux 的包管理器安装 Go，然后编译 📱

```bash
# 在 Termux 中
pkg update
pkg install golang git
cd ~/Termux脚本
export GOPROXY="https://goproxy.cn,direct"
go mod download
go build -ldflags="-s -w" -o fatalder-termux main.go
```

---

## 推荐顺序：

1. **方案一**（Termux 中编译）- 最简单
2. **方案三**（使用代理）- 如果网络可以解决
3. **方案二**（WSL）- 如果你有 WSL
4. **方案八**（Termux 手动）- 如果脚本有问题
