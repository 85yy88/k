# 使用示例

## 📱 Termux 中的实际使用场景

### 场景 1: 转换 Java 版结构到基岩版

```bash
# 从 Java 版服务器下载的结构文件，转换为基岩版格式
fatalder convert \
  /sdcard/Download/castle.schematic \
  MCStructure \
  /sdcard/Download/castle.mcstructure

# 输出: castle.mcstructure
```

### 场景 2: 批量转换多个文件

```bash
# 使用循环批量转换
for file in /sdcard/Download/*.schematic; do
    fatalder convert "$file" MCStructure "${file%.schematic}.mcstructure"
done
```

### 场景 3: 生成大型地图画

```bash
# 将照片转换为 4x4 地图画（16 张地图）
fatalder mapart \
  /sdcard/Pictures/my_photo.jpg \
  /sdcard/games/com.mojang/minecraftWorlds/MyWorld \
  --width 4 \
  --height 4 \
  --x 10 \
  --y -4 \
  --z 10
```

### 场景 4: 解密网易版世界用于编辑

```bash
# 解密网易版世界，然后可以用其他工具编辑
fatalder decrypt \
  /sdcard/games/com.netease/minecraftWorlds/World1

# 解密后，db 目录已解密，可以正常访问
```

### 场景 5: 转换后重新加密

```bash
# 1. 解密
fatalder decrypt world.mcworld

# 2. 编辑（使用其他工具）

# 3. 重新加密
fatalder encrypt world.decrypted.mcworld
```

### 场景 6: 快速查看支持的格式

```bash
# 查看所有支持的格式
fatalder list

# 输出示例:
# 支持的格式:
#   1. AxiomBP
#   2. BDX
#   3. Construction
#   4. KBDX
#   5. Litematic
#   6. MCStructure
#   7. MCWorld
#   8. MCFunction
#   9. SchemV1
#   10. SchemV2
#   11. Schematic
#   ...
```

## 🎨 地图画高级用法

### 生成平面 2D 地图画

```bash
fatalder mapart \
  /sdcard/Download/logo.png \
  world.mcworld \
  --width 1 \
  --height 1 \
  --2d
```

### 生成大型 3D 地图画

```bash
fatalder mapart \
  /sdcard/Download/artwork.jpg \
  world.mcworld \
  --width 3 \
  --height 3 \
  --x 0 \
  --y -4 \
  --z 0
```

## 🔄 格式转换链

### 示例: Schematic → BDX → MCStructure

```bash
# 步骤 1: Schematic 转 BDX
fatalder convert input.schematic BDX temp.bdx

# 步骤 2: BDX 转 MCStructure
fatalder convert temp.bdx MCStructure output.mcstructure

# 清理临时文件
rm temp.bdx
```

## 💡 实用技巧

### 技巧 1: 创建别名

在 `~/.bashrc` 中添加：

```bash
alias mc-convert='fatalder convert'
alias mc-mapart='fatalder mapart'
alias mc-encrypt='fatalder encrypt'
alias mc-decrypt='fatalder decrypt'
```

然后：
```bash
source ~/.bashrc
mc-convert file.schematic MCStructure
```

### 技巧 2: 使用脚本自动化

创建 `convert-all.sh`:

```bash
#!/bin/bash
INPUT_DIR="/sdcard/Download/structures"
OUTPUT_DIR="/sdcard/Download/converted"

mkdir -p "$OUTPUT_DIR"

for file in "$INPUT_DIR"/*.schematic; do
    if [ -f "$file" ]; then
        name=$(basename "$file" .schematic)
        echo "转换: $name"
        fatalder convert "$file" MCStructure "$OUTPUT_DIR/$name.mcstructure"
    fi
done

echo "完成！"
```

### 技巧 3: 检查文件格式

```bash
# 尝试转换，如果格式不支持会报错
fatalder convert unknown.file UnknownFormat 2>&1 | grep "不支持"
```

## 🚨 常见错误处理

### 错误: "无法识别源文件格式"

```bash
# 检查文件是否存在
ls -lh /sdcard/Download/file.schematic

# 检查文件类型
file /sdcard/Download/file.schematic

# 尝试其他格式名称
fatalder list  # 查看支持的格式
```

### 错误: "无法打开世界"

```bash
# 检查路径是否正确
ls -la /sdcard/games/com.mojang/minecraftWorlds/

# 确保有读取权限
# 在 Termux 中可能需要授予存储权限
```

### 错误: "存储空间不足"

```bash
# 检查可用空间
df -h /sdcard

# 清理临时文件
rm -rf /data/data/com.termux/files/usr/tmp/*
```

## 📊 性能优化

### 大文件转换

对于大文件，可以考虑：

1. **使用更快的存储位置**
   ```bash
   # 使用内部存储而不是 SD 卡
   fatalder convert /data/data/com.termux/files/home/file.schematic MCStructure
   ```

2. **分批处理**
   ```bash
   # 将大文件分成多个小文件处理
   ```

3. **后台运行**
   ```bash
   # 使用 nohup 后台运行
   nohup fatalder convert large.schematic MCStructure &
   ```

## 🎯 完整工作流示例

### 从 Java 版结构到基岩版地图画

```bash
# 1. 转换格式
fatalder convert java_building.schematic MCStructure building.mcstructure

# 2. 导入到世界（使用其他工具或手动）

# 3. 生成地图画展示
fatalder mapart \
  /sdcard/Pictures/building_photo.jpg \
  /sdcard/games/com.mojang/minecraftWorlds/MyWorld \
  --width 2 \
  --height 2
```
