# 快速开始指南

## 🚀 3 步开始使用

### 步骤 1: 在 Termux 中安装 Go

```bash
pkg update && pkg upgrade
pkg install golang
```

### 步骤 2: 编译二进制文件

```bash
# 进入项目目录
cd ~/fatalder-termux/Termux脚本

# 编译（会自动下载依赖）
chmod +x build.sh
./build.sh
```

### 步骤 3: 使用工具

```bash
# 给二进制文件执行权限
chmod +x fatalder-termux

# 查看帮助
./fatalder-termux help

# 开始使用
./fatalder-termux convert input.schematic MCStructure
```

## 📦 或者直接使用预编译版本

如果你有预编译的二进制文件：

```bash
# 下载二进制文件
wget <下载链接> -O fatalder-termux

# 给执行权限
chmod +x fatalder-termux

# 移动到 PATH（可选，方便使用）
mv fatalder-termux ~/../usr/bin/fatalder

# 现在可以在任何地方使用
fatalder help
```

## 🎯 常用命令

```bash
# 转换结构文件
fatalder convert input.schematic MCStructure output.mcstructure

# 生成地图画
fatalder mapart image.jpg world.mcworld --width 2 --height 2

# 解密网易存档
fatalder decrypt world.mcworld

# 列出支持的格式
fatalder list
```

## ✅ 完成！

现在你可以使用所有功能了：
- ✅ 结构格式转换（已解除限制）
- ✅ 地图画转换（已解除限制）
- ✅ 存档加密/解密（已解除限制）
